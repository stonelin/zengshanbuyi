# 部署目标（make deploy 使用）
# REMOTE      ssh 目标：~/.ssh/config 里的主机别名，或 user@host
# REMOTE_PATH 远程站点根目录，rsync --delete 会全量同步到这里
# SITE_URL    站点地址，仅用于发布后打印
REMOTE=your-ssh-host
REMOTE_PATH=/var/www/your-site
SITE_URL=http://your-site.example.com
